# 📋 تقرير تسليم المشروع وتفاصيل الجلسة (SESSION.md)
## مشروع متجر Hi Line Pro Care الإلكتروني 🛍️

يركز هذا المستند على تسليم حالة المشروع وتوضيح كافة التفاصيل الأمنية والبرمجية والخطوات المستقبلية الآمنة لمديري ومطوري النظام.

---

## 1️⃣ الأعمال المنجزة (Completed Work)

### 🔒 الحماية والأمان (Security)
* **حماية استجابة تسجيل الدخول:** يستخدم الخادم `passwordHash` داخلياً للتحقق من كلمة المرور، ثم يحذفه صراحةً من استجابة `localUserLogin` قبل إعادة بيانات المستخدم للعميل.
* **محدد معدل الطلبات (Rate Limiting):** دمج برمجية وسيطة (Middleware) لتحديد معدل الطلبات على مسارات الـ API لحماية قاعدة البيانات من الاستخدام المكثف بقيمة حد أقصى 300 طلب لكل دقيقة (و10 طلبات للمسارات الحساسة مثل تسجيل الدخول وإنشاء الطلب).
  > [!IMPORTANT]
  > هذا المحدد يعمل بالكامل داخل الذاكرة (In-Memory) وهو محدود بـ (Single-Process). لذا فهو غير موزع (Not Distributed)؛ وفي حال تشغيل خوادم متعددة خلف موازن حمل (Load Balancer)، ستكون حدود العمليات معزولة لكل نسخة خادم على حدة وليس عبر قاعدة بيانات مشتركة مثل Redis.
* **الترويسات الأمنية (Secure Headers):** تهيئة خادم Hono لإضافة ترويسات حماية مثل `X-Content-Type-Options: nosniff` و `X-Frame-Options: DENY` و `Strict-Transport-Security` في بيئة الإنتاج.
* **التحقق من التوقيع الرقمي (Timing-Safe HMAC Verification):** تفعيل ميزة التحقق الآمن زمنياً من HMAC القادم من بوابة Paymob لمنع هجمات التزييف الرقمي للمدفوعات.

### 💳 خيارات الدفع (Payment Options)
* **بوابة Paymob:** الكود البرمجي جاهز ومعد بالكامل لبوابة الدفع بالبطاقات البنكية، ويشمل ذلك إنشاء جلسة الدفع، وإنشاء الرابط الخاص بـ Iframe، ومعالجة الـ Callback Webhook لتحديث حالة الطلب إلى "مدفوع" بأمان وتأمين حركة المخزون.
  > [!WARNING]
  > بوابة Paymob جاهزة برمجياً ولكنها غير مفعلة وغير مختبرة عملياً بشكل حي نظراً لعدم توفر مفاتيح الإنتاج وحساب العميل والرابط العام للـ Webhook.
* **إرجاع المخزون وإلغاء الكوبونات:** النظام مهيأ لإلغاء المعاملات الفاشلة وترميم المخزون المستقطع وإلغاء استهلاك الكوبون في حال فشل الدفع لضمان دقة وتطابق البيانات.
* **طرق الدفع اليدوية (Vodafone Cash & InstaPay):** تظهر للمستخدم شاشات توضح بيانات التحويل (مثل رقم محفظة فودافون كاش أو اسم حساب إنستاباي) في صفحة تأكيد الطلب، مع توفير زر ينشئ رابط واتساب مباشر (wa.me) ليقوم العميل من خلاله بمراسلة الدعم يدوياً وإرسال صورة إيصال التحويل لتأكيد الطلب. لا توجد برمجية لرفع الملفات أو الإيصالات مباشرة عبر الموقع.

### 🗄️ قاعدة البيانات (Database)
* **مخطط الجداول وهيكلتها:** استخدام Drizzle ORM لإنشاء وهجرة الجداول مع توفير دعم كامل للغة العربية عبر استخدام الترميز `utf8mb4`.
* **سجل المخزون:** توثيق حركة الصادرات والواردات يدوياً وتلقائياً عبر جدول `inventory_movements`.

### 🎨 واجهة المستخدم (UI & UX)
* تصميم متجاوب لمتجر Hi Line Pro Care مبني بـ React يدعم تخطيط RTL للعربية و LTR للإنجليزية.
* لوحة تحكم إدارية تمكن من إدارة الطلبات والمنتجات وتعديل المحافظات وقيم الشحن وطباعة فواتير A4 وملصقات شحن 10x15 سم.

### 📈 تحسين محركات البحث الأساسي (Basic Client-Rendered SEO)
* **العناوين والوصف والروابط الصديقة:** استخدام مكتبة `react-helmet-async` لإدارة العناوين (Titles) والأوصاف (Meta Descriptions) ووسوم الرابط الأساسي (Canonical URL) ديناميكياً على مستوى متصفح العميل.
* **منع الأرشفة للصفحات الخاصة:** إضافة وسوم `noindex, nofollow` لمسارات السلة والدفع والحساب وتأكيد الطلب (`/cart` و`/checkout` و`/account` و`/order-confirmation`).
* **ضبط الزحف والـ Sitemap:** إضافة `robots.txt` لمنع زحف محركات البحث إلى API ولوحة الإدارة وصفحات المصادقة والشراء الخاصة، وإضافة `noindex, nofollow` مباشرة إلى صفحات الإدارة واستعادة كلمة المرور. كما يوفر الخادم `/sitemap.xml` ديناميكياً باستخدام عنوان الطلب والمنتجات النشطة، لذلك يتكيف تلقائياً مع الدومين النهائي.
* **البيانات المهيكلة (JSON-LD):** دمج ترميز `Organization` في الصفحة الرئيسية وترميز `Product` الديناميكي في صفحات المنتجات، شاملاً السعر والعملة والصورة والعلامة التجارية وحالة التوفر.
  > [!NOTE]
* **مسارات اللغات وhreflang:** فصل واجهة المتجر إلى مسارات عربية `/ar/...` وإنجليزية `/en/...`، مع تحويل الروابط القديمة تلقائياً إلى اللغة المحفوظة، وإضافة canonical ذاتي وروابط `hreflang` متبادلة للغتين و`x-default` للإنجليزية.

---

## 2️⃣ البوابات التي تم التحقق من عملها (Verified Gates)

تم اختبار وظائف التطبيق وضمان سلامة التدفقات البرمجية من خلال **38 اختباراً في 9 ملفات اختبارية** تعمل بواسطة مكتبة Vitest (`npm run test`):
1. **التحقق من webhook الخاص بـ Paymob وبصمة HMAC (عدد 7 اختبارات في `boot.test.ts`):** التحقق من سلامة استيعاب وتمرير التواقيع الأمنية والتصدي للمطابقات المغلوطة للمبالغ والعملات.
2. **المصادقة الإدارية وجلسات المشرفين (عدد 3 اختبارات في `auth.test.ts`):** التحقق من حظر الطلبات المجهولة وقبول ملفات تعريف الارتباط الموقعة للمشرفين.
3. **تكرار إنشاء الطلبات وترميم المخزون (عدد 4 اختبارات في `store-router.test.ts`):** التحقق من الحماية ضد تكرار الطلبات عند الضغط المتعدد (Idempotency Key) وترميم المخزون عند فشل دفع عملية Paymob.
4. **منطق عمل سلة المشتريات (عدد 4 اختبارات في `useCart.test.ts`):** التحقق من إضافة وحساب العناصر ديناميكياً وتعديل كمياتها وحساب السعر الإجمالي بشكل صحيح.
5. **تحديد معدل الطلبات (اختباران في `rate-limiter.test.ts`):** التحقق من ترويسات الحد، واستجابة `429` و`Retry-After`، وإعادة السماح بعد انتهاء النافذة الزمنية.
6. **التحقق من الكوبونات (5 حالات إضافية في `store-router.test.ts`):** التحقق من سقف الخصم الثابت ورفض الكوبون غير النشط أو المنتهي أو المستنفد أو غير المستوفي للحد الأدنى للطلب.
7. **مستندات الطباعة (3 حالات في `printDocuments.test.ts`):** التحقق من فاتورة A4، وملصق شحن 10×15 سم للدفع عند الاستلام والدفع المسبق، مع ترميز بيانات العميل والمنتجات لمنع حقن HTML داخل نافذة الطباعة.
8. **SEO المنتجات (حالتان في `productSeo.test.ts`):** التحقق من Product JSON-LD، وروابط الصور المطلقة، والسعر بالجنيه المصري، وحالتي توفر المخزون.
9. **خريطة الموقع (حالة في `sitemap.test.ts`):** التحقق من الصفحات العامة، وروابط المنتجات المشفرة، وتاريخ التعديل، واستبعاد المسارات الخاصة.
10. **توجيه اللغات (7 حالات في `localeRouting.test.ts`):** التحقق من قراءة اللغة من الرابط، وتبديل اللغة مع الحفاظ على المسار، وتحويل المسارات القديمة، وإزالة بادئة اللغة بأمان.

> [!NOTE]
> تم اختبار محتوى مستندات الطباعة ومقاساتها آلياً؛ تبقى معاينة الطباعة الفعلية على طابعة A4 وطابعة ملصقات 10×15 خطوة تشغيلية يدوية تعتمد على تعريفات الطابعة والمتصفح.

---

## 📂 3️⃣ موقع النسخ الاحتياطي (Backup Location)

توجد نسختان احتياطيتان لقاعدة البيانات MySQL محفوظتان بأمان في مجلد المشروع الرئيسي:
* المسار: [backups/](file:///E:/Projects/Web/Hi%20Line%20Pro%20Care%20E-commerce/backups/)
* الملف الأول: `hiline_pro_care-before-payment-migration-14480203-200809.sql`
* الملف الثاني (النسخة المحمولة): `hiline_pro_care-before-payment-migration-portable-20260717-200850.sql`

---

## ⚙️ 4️⃣ القرارات الخارجية المتبقية (Remaining External Decisions)

يتطلب إطلاق المتجر للجمهور توفير وحسم المدخلات والبيانات التالية:
1. **بيانات Paymob للإنتاج:** استخراج المعرفات الخاصة بالـ Integration والـ Iframe والمفاتيح السرية للتوقيع HMAC وإدراجها في `.env`.
2. **ربط رقم WhatsApp:** روابط التواصل اليدوي عبر WhatsApp تعمل من الواجهة. أما الإشعارات التلقائية فتحتاج تنفيذًا واختبارًا منفصلين قبل اعتبارها مفعلة.
3. **اسم النطاق والاستضافة (Domain & Hosting):** اختيار اسم النطاق، وتهيئة خادم استضافة مستقر يدعم ترميز اللغة العربية بالكامل لتشغيل تطبيق Node.js وربطه بقاعدة بيانات MySQL حية.

---

## 🛡️ 5️⃣ الخطوات التالية الآمنة (Safe Next Steps)

1. **الاستضافة التجريبية (Staging Env):** رفع التطبيق وتشغيل قاعدة البيانات باستخدام النسخة الاحتياطية المحمولة من المجلد `backups` للتحقق من سلامة البناء في خادم حقيقي.
2. **تشغيل Paymob في بيئة الاختبار (Sandbox Mode):** اختبار تدفق الدفع وبصمة HMAC باستخدام بطاقات تجريبية للتحقق من نجاح عملية التحديث التلقائي للطلبات قبل إدخال مفاتيح الإنتاج الحية.
3. **تزويد مفاتيح Meta Pixel & CAPI:** ربط المتجر بإعلانات فيسبوك عن طريق تزويد توكن الوصول الحي للمنصة في بيئة الإنتاج لتعقب عمليات الشراء.
