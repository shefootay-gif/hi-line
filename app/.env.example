# ── Backend ─────────────────────────────────────────────────────
JWT_SECRET=local-dev-jwt-secret-change-this

# ── Database ───────────────────────────────────────────────────
# MySQL connection string (edit user/password/database if your local MySQL differs)
DATABASE_URL=mysql://hiline_dev:HiLineDev123!@127.0.0.1:3306/hiline_pro_care

# ── Admin Role ──────────────────────────────────────────────────
LOCAL_ADMIN_USERNAME=admin
LOCAL_ADMIN_PASSWORD=change-this-admin-password

# ── Kimi OAuth (OPTIONAL - only needed if using OAuth login) ────
APP_ID=                   # Optional: Kimi OAuth Application ID
APP_SECRET=               # Optional: Kimi OAuth secret
VITE_KIMI_AUTH_URL=       # Optional: Kimi OAuth server URL
VITE_APP_ID=              # Optional: OAuth application ID (frontend)
KIMI_AUTH_URL=            # Optional: Kimi OAuth server URL (backend)
KIMI_OPEN_URL=            # Optional: Kimi Open Platform URL
OWNER_UNION_ID=           # Optional: Union ID for admin promotion on first login
