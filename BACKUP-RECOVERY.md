# النسخ والاستعادة الآمنة

من لوحة التحكم > تصدير ونسخ، أنشئ نسخة بكلمة تشفير لا تقل عن 12 حرفًا. يتم تنزيل ملف `.hlbackup` مشفر بـAES-256-GCM ومفتاح مشتق عبر scrypt. احتفظ بالملف خارج الاستضافة، وبكلمة التشفير بشكل منفصل؛ فقدها يمنع الاستعادة. كلمة التشفير لا تُخزن في قاعدة البيانات أو السجلات.

النسخة تحتوي جميع جداول قاعدة البيانات (ومنها عناصر الطلبات والمخزون والمستخدمون) وملفات `dist/public` المحلية، وتشمل الصور المرفوعة المخزنة في قاعدة البيانات. لا تشمل `.env` أو أسرار السيرفر أو الصور المستضافة خارجيًا. احتفظ بإعدادات السيرفر الآمنة ونسخة المشروع بشكل منفصل. يلزم عدم تغيير مخطط قاعدة البيانات أثناء النسخ؛ يتم التقاط جداول InnoDB من معاملة متسقة.

توجد نسخة مشفرة إضافية في `private-backups` خارج المجلد العام وخارج قاعدة البيانات، وتبقى بعد نشر الكود. بقاء الملف على نفس الخادم ليس بديلًا عن تنزيله خارجيًا. لا يوجد حذف تلقائي للنسخ؛ راقب مساحة الاستضافة.

## التحقق والاستعادة

بعد بناء المشروع توجد أداة `app/dist/restore-backup.js`. اضبط متغيرات البيئة في جلسة طرفية خاصة؛ لا تضع كلمات المرور في Git أو في سجل أوامر مشترك:

- `BACKUP_FILE`: مسار النسخة المشفرة.
- `BACKUP_PASSPHRASE`: كلمة تشفير النسخة.

شغّل `node dist/restore-backup.js --verify` من مجلد app للتحقق من كلمة المرور وسلامة الأرشيف دون كتابة أي بيانات.

للاستعادة، أنشئ قاعدة MySQL جديدة فارغة، ثم اضبط:

- `RESTORE_DATABASE_URL`: اتصال القاعدة الجديدة، باسم مختلف عن المصدر.
- `RESTORE_ASSET_DIR`: مجلد جديد غير موجود لاستعادة الملفات العامة إليه.
- `RESTORE_CONFIRM`: القيمة `RESTORE_TO_EMPTY_DATABASE`.

شغّل `node dist/restore-backup.js`. الأداة ترفض القاعدة غير الفارغة أو اسم قاعدة المصدر أو مجلد ملفات موجود؛ لا تحذف ولا تستبدل الإنتاج. عند فشل جزئي، تبقى قاعدة الاختبار فقط متأثرة؛ افحصها قبل محاولة جديدة بوجهة فارغة جديدة.

بعد الاستعادة افحص أعداد المنتجات والطلبات وعناصرها والمخزون وملفات الصور في بيئة منفصلة. لا تبدّل اتصال الإنتاج إلى القاعدة المستعادة إلا بعد مراجعة الفرق مع أحدث الطلبات وموافقة المالك. لا تستعد ملفات من مصدر غير موثوق حتى لو كنت تعرف كلمة تشفيرها.

حدود النسخة عبر اللوحة: 200 MB قبل الضغط للبيانات والملفات مجتمعة، و100 MB للملفات العامة. للمتاجر الأكبر استخدم نسخة استضافة كاملة وأداة MySQL المتخصصة.
